FROM centos:centos6
MAINTAINER jeff.tunessen@gmail.com

#ADD http://dl.fedoraproject.org/pub/epel/6/x86_64/epel-release-6-8.noarch.rpm /tmp/epel-release.rpm

#RUN rpm -ivh /tmp/epel-release.rpm \
#    && yum repolist \
#    && yum -y install ansible \
#    && yum clean all

RUN yum -y install epel-release \
    && yum repolist \
    && yum -y install ansible \
    && yum clean all

# sleep infinity, use container as running system for testing ansible roles
CMD ["tail", "-f", "/dev/null"]